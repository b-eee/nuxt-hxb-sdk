//get reports
import {createClient} from "@hexabase/hexabase-js";

async function getReports(projectId: any) {
    // const {application, error} = await hexabase.getReports(projectId)
    // return application
}

//get data reports
async function getDataReport(projectId: any, reportId: any, reportDataPayload: any) {
    // const {dataReport, error} = await hexabase.applications.getDataReport(projectId, reportId, reportDataPayload)
    // return dataReport
}
